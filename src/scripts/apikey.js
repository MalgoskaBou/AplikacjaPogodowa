export default () => {
    // if (process.env.NODE_ENV !== 'production') {
    //     require('dotenv').config()
    // }
    // console.log("Show me a KEY Api: " + JSON.stringify(process.env));
    return 'ea0bbc74362141bcef9e2e20bff7ec0f'
    //process.env.API_KEY;
}